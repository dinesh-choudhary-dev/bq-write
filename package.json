{
  "name": "bq-write-monorepo",
  "private": true,
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "cli": "npm run dev --workspace=apps/cli",
    "web": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
